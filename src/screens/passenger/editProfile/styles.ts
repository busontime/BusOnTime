import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputTextPicker: {
    minWidth: '98%',
    borderRadius: 8,
    backgroundColor: '#111111',
    margin: 6,
  },

  textPicker: {
    marginLeft: 14,
    borderRadius: 6,
    minWidth: '100%',
  },
});
